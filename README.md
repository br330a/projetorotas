# Pacote de dependências para criar rotas

npm install react-router-dom

# Estrutura do router

# Browser Router /Router
prepara o navegador para receber as rotas

# Routes
prepara a estrutura para receber a rota

# Route
é a rota que vai ser chamada no navegador
Ex: Home, Sobre, contato

# Pacote de dependencias para icobnes
npm install lucide-react